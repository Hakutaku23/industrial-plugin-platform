# Binary Plugin Contract

Binary plugins should expose a controlled CLI entry. The runner will pass a
standard JSON execution payload through stdin or an input file and will parse
standard JSON from stdout or an output file.

