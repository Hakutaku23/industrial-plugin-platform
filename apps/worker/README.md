# Worker

This directory is reserved for scheduling and background execution.

The local MVP should start with APScheduler only after the package registry,
manifest validation, and local runner contracts are stable.

